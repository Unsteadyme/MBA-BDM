PROBLEM STATEMENT 1



SELECT CASE
    WHEN G_age < 25 THEN 'Under 25'
    ELSE '25 and Over'
    END AS age_group, AVG(DATEDIFF(check_out, check_in_)) AS avg_stay
FROM Reservation re JOIN Guest_details g ON re.guest_id = g.guest_id
GROUP BY age_group;
